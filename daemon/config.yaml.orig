# LinuxMonitor daemon configuration
interval_seconds: 60            # sample-interval
retention_days: 31              # removal of old records (NULL = off)
log_level: info                 # debug, info, warning, error, critical

mysql:
  host: localhost
  port: 3306
  database: linuxmonitor
  user: linmon_writer
  password: WRITE31f34ert4ggs

dbCleanup:
  toQuarter: 10
  toHour: 20
  toDay: 40

# Host label (to distingish between servers)
host_label: "System1"
